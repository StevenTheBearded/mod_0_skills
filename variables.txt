var food = "falafel";
var isDelicious = true;

food = "hummus"; // removed var from syntax

var num2 = 32;

var num7 = 14;  {
console.log(num7 / num2)
}

// Added after assesment timed out. Don't remember question but it asked for quotient
// of num variables to be divided in a string?
var quotient = num2 / num7
console.log("The quotient is " + quotient)



var songs = ["Nikes", "Ivy", "Novacane"] //added brackets to make it an array
var newSongs = songs.shift()

console.log(food.length)